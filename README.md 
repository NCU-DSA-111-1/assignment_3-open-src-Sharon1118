# Arithmetic coding v.s. Huffman coding

## Introduction
自行建立一個文字檔，並將現有的開源程式碼應用到文字檔中，最後印出程式執行時間，單位是毫秒。

## Compile arcd
第一步:cd arcd    (打開homework3資料夾中的arcd)

第二步:make(編譯)


## Run arcd
第一步:cd examples   (打開homework3資料夾中的arcd中的examples)

第二步-壓縮:./arcd_stream -e < input.file | tee output.file 

(我的input.file為text.txe ; output.file為sharon.txt)

第三步-解壓縮:./arcd_stream -d < input.file | tee output.file

(我的input.file為sharon.txe ; output.file為hi.txt)

## Compile huffman

第一步:make   (編譯)



## Run huffman

第一步-壓縮:./huffcode -i input.file -o output.file -c 

(我的input.file為huff.txe ; output.file為output.txt)

第二步-解壓縮:./huffcode -i input.file -o output.file -d

(我的input.file為output.txe ; output.file為huffman.txt)
